name: cypress-test

on: deployment

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: mikefrancis/vercel-cypress@v1
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          repo: opspy.dev